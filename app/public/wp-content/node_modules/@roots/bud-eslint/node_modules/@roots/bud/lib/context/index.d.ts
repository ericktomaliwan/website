import type { Context } from '@roots/bud-framework/context';
export type Options = {
    extensions?: Array<string>;
} & Omit<Partial<Context>, `extensions`>;
export default function make(options?: Options): Promise<Context>;
export type { Context };
