import { jsx as _jsx } from "@roots/bud-support/jsx-runtime";
import { LabelBox } from '@roots/bud/cli/components/LabelBox';
import { Box, Text } from '@roots/bud-support/ink';
export const Children = ({ compilers, }) => {
    if (!compilers)
        return null;
    const labels = Object.values(compilers).map((child, i) => child.label);
    return (_jsx(LabelBox, { label: "Child compilers", children: _jsx(Box, { flexDirection: "column", children: labels.map((label, i) => (_jsx(Text, { children: label }, i))) }) }));
};
