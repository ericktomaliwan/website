import type { Bud, Items } from '@roots/bud-framework';
import type { Item } from '@roots/bud-framework/services/build';
import { Extension } from '@roots/bud-framework/extension';
/**
 * resolve-url-loader configuration
 */
export default class BudResolveUrl extends Extension {
    loaderPath?: false | string;
    /**
     * {@link Extension.register}
     */
    register({ build, hooks, path }: Bud): Promise<import("@roots/bud-support/lodash/logger").Logger | undefined>;
    /**
     * {@link Extension.boot}
     */
    boot({ build }: Bud): Promise<void>;
    /**
     * Callback for {@link Bud.hooks} `build.resolveLoader.alias`
     */
    onBuildResolveLoaderAlias(aliases?: Record<string, string>): Record<string, string>;
    withResolveLoader(use: Array<Item | keyof Items>): Array<Item | keyof Items>;
}
