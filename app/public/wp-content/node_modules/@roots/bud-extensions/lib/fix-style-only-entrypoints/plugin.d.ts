import type { Compiler, WebpackPluginInstance } from '@roots/bud-framework/config';
import { Extension } from '@roots/bud-framework/extension';
/**
 * Remove empty modules from the compilation
 */
export default class FixStyleOnlyEntrypoints extends Extension implements WebpackPluginInstance {
    /**
     * {@link WebpackPluginInstance.apply}
     */
    apply({ hooks }: Compiler): void;
}
